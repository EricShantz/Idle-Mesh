Intro Tutorial Slides:
- slide 1: instead of a glowing ring around the publisher button, the publisher should scale and show the cooldown bar as it does on the actual canvas, and the dots coming out of it should be synced with the "clicks"
- slide 2: there should be a broker component between the publisher and subscriber
- slide 3: the dots should be coming in from the left and stopping on the left edge of the subscriber component as it does in the actual canvas. The money incremement animation should be centered above the subsrciber component and it should have the coin icon as it does in the actual canvas
- slide 4: the blue highlight circle isnt centered around the upgrade arrow. The "shop" list on the right should look like and contain what the actual queue upgrade modal looks like.

Broker Slide:
- this grahic should show a broker component in the center with multiple connection lines coming in and out of it (normal connection lines and bridge lines) - note that the other components its connected to shouldnt be shown - just the lines that fade at the edge of the graphic
- this graphic should also show broker fanout, so 1 event comes in, and it should exit on multiple lines

Queue Slide:
- the 3 slots icons on the queue are too high and are covering the word "queue". they should be beneath the word queue like on a real queue component
- the 3 slot icons are puslsing like a loading icon, they should be flickering on and off how a real queue's slot display works

Dead Message Queue Slide:
- The connection line should be orthogonal to the broker, not diagonal
- the red dots should be falling from above the DMQ
- the DMQ component should have slots like the real DMQ, and events should move up the connection line back to the broker

